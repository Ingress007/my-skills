# app-deploy-ops 全局配置
app_name: app-deploy-ops
workspace_dir: ~/.app-deploy-ops
log_level: INFO

# SSH 连接配置（读取 ~/.ssh/config）
ssh:
  default_timeout: 30
  connect_timeout: 10

# 默认预设
default_preset: spring-boot-vue

# 备份保留数量
backup:
  max_backups: 10